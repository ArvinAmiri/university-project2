#include <iostream>
using namespace std;
    class peaple
    {
        private:
         double mohasebeage {
             int tedad;
             int names [tedad]={arvin,ali,majid}
             int age[tedad]={18,19,20}
             int totalage;
             for(int i=0,i<tedad,i++){
                 totalage +=age[i];
             }
         }
        
 }
    public:
    double avg=mohasebeage();
    }
